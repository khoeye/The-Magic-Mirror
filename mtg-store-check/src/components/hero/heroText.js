import React, { Fragment } from "react";
import styles from "./hero.module.css"

const HeroText = (props) => {
    return(
<Fragment>
    <p>Test of the text</p>
</Fragment>
    )
}

export default HeroText
