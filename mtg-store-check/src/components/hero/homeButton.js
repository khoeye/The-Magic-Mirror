import React, { Fragment } from "react";

const HomeButton = (props) => {
    return (
        <Fragment>
            <button>MTG Magic Mirror</button>
        </Fragment>
    )
}

export default HomeButton